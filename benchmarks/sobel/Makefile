SRC := sobel

STANDARD_ARGS := buffalo.pgm
PERFORATED_ARGS := buffalo.pgm

RUN_AFTER_STANDARD := mv out.pgm output-standard.pgm
RUN_AFTER_PERFORATED := mv out.pgm output-perforated.pgm

include ../../Makefile